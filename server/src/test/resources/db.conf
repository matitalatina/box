db {
  url="jdbc:postgresql:boxserver"  
  user="postgres"
  password="postgres",
  schema="public",
  generator{
    tables=["*"],
    views=["*"],
    excludes=[],    //for tables anor views
    excludeFields=[],
    keys {    //specify if key oninserts are managed by dbms (db) or application (app)
      default.strategy="app"
      db=[]
      app=[]
    }
  }
}
